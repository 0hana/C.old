name: build and test
on: push
jobs:
  build-and-test:
    name: build and test
    runs-on: ubuntu-latest
    steps:
      - name: git clone 0hana/C
        uses: actions/checkout@v2

      - run: make
